// Centralized layout constants for timeline pages
export const APP_HEADER_HEIGHT = 72; // pixels
export const APP_OUTER_PADDING = 6; // pixels (equals Tailwind p-1.5)
export const MIN_COLUMN_WIDTH_NO_SCROLL = 30; // minimum width before allowing vertical-only scroll
export const MAX_CONTAINER_WIDTH_RATIO = 0.98; // use 98% of viewport to account for borders
